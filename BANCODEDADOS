-- Criar Banco de Dados
CREATE DATABASE IF NOT EXISTS ordens_servico;
USE ordens_servico;

-- ===============================
-- 1. Tabela Clientes
-- ===============================
CREATE TABLE clientes (
    id_cliente INT AUTO_INCREMENT PRIMARY KEY,
    nome_cliente VARCHAR(100) NOT NULL,
    contato VARCHAR(100),
    telefone VARCHAR(20),
    endereco VARCHAR(150),
    numero VARCHAR(10),
    bairro VARCHAR(50),
    cidade VARCHAR(50),
    uf CHAR(2),
    codigo_cliente VARCHAR(20)
);

-- ===============================
-- 2. Tabela Técnicos
-- ===============================
CREATE TABLE tecnicos (
    id_tecnico INT AUTO_INCREMENT PRIMARY KEY,
    nome_tecnico VARCHAR(100) NOT NULL,
    telefone VARCHAR(20),
    email VARCHAR(100),
    nome_ajudante VARCHAR(100),
    telefone_ajudante VARCHAR(20),
    senha VARCHAR(100),
    valor_hora_trabalhada DECIMAL(10,2) DEFAULT 50.00,
    valor_deslocamento DECIMAL(10,2) DEFAULT 30.00,
    valor_por_km DECIMAL(10,2) DEFAULT 0.75,
    valor_hora_extra DECIMAL(10,2) DEFAULT 75.00
);

-- ===============================
-- 3. Tabela Ordens de Serviço
-- ===============================
CREATE TABLE ordens_servico (
    id_os INT AUTO_INCREMENT PRIMARY KEY,
    id_cliente INT NOT NULL,
    id_tecnico INT NOT NULL,
    pedido VARCHAR(50),
    data_faturamento DATE,
    garantia BOOLEAN DEFAULT 0,
    empresa VARCHAR(100),
    cidade_empresa VARCHAR(50),
    uf_empresa CHAR(2),
    data_abertura DATE NOT NULL,
    descricao_chamado TEXT,
    observacoes_cliente TEXT,
    data_primeira_visita DATETIME,
    data_segunda_visita DATETIME,
    servico_finalizado BOOLEAN DEFAULT 0,
    status VARCHAR(20) DEFAULT 'pendente',  -- pendente, em_andamento, aguardando_peca, finalizada, cancelada
    pendencia TEXT,
    observacoes_tecnico LONGTEXT, 
    assinatura_base64 LONGTEXT, 
    FOREIGN KEY (id_cliente) REFERENCES clientes(id_cliente),
    FOREIGN KEY (id_tecnico) REFERENCES tecnicos(id_tecnico)
);

-- ===============================
-- 4. Tabela Defeitos
-- ===============================
CREATE TABLE defeitos (
    id_defeito INT AUTO_INCREMENT PRIMARY KEY,
    id_os INT NOT NULL,
    categoria VARCHAR(50),
    descricao VARCHAR(100),
    FOREIGN KEY (id_os) REFERENCES ordens_servico(id_os)
);

-- ===============================
-- 5. Tabela Materiais Utilizados
-- ===============================
CREATE TABLE materiais_utilizados (
    id_material INT AUTO_INCREMENT PRIMARY KEY,
    id_os INT NOT NULL,
    nome_material VARCHAR(100) NOT NULL,
    quantidade DECIMAL(10,2),
    valor_unitario DECIMAL(10,2),
    valor_total DECIMAL(10,2),
    FOREIGN KEY (id_os) REFERENCES ordens_servico(id_os)
);

-- ===============================
-- 6. Tabela Fotos OS
-- ===============================
CREATE TABLE fotos_os (
    id_foto INT AUTO_INCREMENT PRIMARY KEY,
    id_os INT NOT NULL,
    tipo VARCHAR(20),  -- antes, depois, equipamento
    url VARCHAR(500),
    descricao VARCHAR(200),
    data_envio DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_os) REFERENCES ordens_servico(id_os)
);

-- ===============================
-- 7. Tabela Geolocalização
-- ===============================
CREATE TABLE geolocalizacao_os (
    id_geo INT AUTO_INCREMENT PRIMARY KEY,
    id_os INT NOT NULL,
    tipo VARCHAR(20),  -- inicio_visita, fim_visita
    latitude DECIMAL(10,8),
    longitude DECIMAL(11,8),
    data_registro DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_os) REFERENCES ordens_servico(id_os)
);

-- ===============================
-- 8. Tabela Chat
-- ===============================
CREATE TABLE chat_mensagens (
    id_mensagem INT AUTO_INCREMENT PRIMARY KEY,
    id_os INT NOT NULL,
    mensagem TEXT,
    remetente VARCHAR(20),  -- tecnico, empresa
    data_envio DATETIME DEFAULT CURRENT_TIMESTAMP,
    lida BOOLEAN DEFAULT 0,
    FOREIGN KEY (id_os) REFERENCES ordens_servico(id_os)
);

-- ====================================================================
-- COMANDOS PARA ATUALIZAR BANCO EXISTENTE (sem perder dados)
-- ====================================================================

-- Adicionar coluna status na tabela ordens_servico (se não existir)
-- ALTER TABLE ordens_servico ADD COLUMN status VARCHAR(20) DEFAULT 'pendente';

-- Alterar data_visita para datetime (se estiver como date)
-- ALTER TABLE ordens_servico MODIFY COLUMN data_primeira_visita DATETIME;
-- ALTER TABLE ordens_servico MODIFY COLUMN data_segunda_visita DATETIME;

-- Adicionar colunas de configuração na tabela tecnicos (se não existirem)
-- ALTER TABLE tecnicos ADD COLUMN valor_hora_trabalhada DECIMAL(10,2) DEFAULT 50.00;
-- ALTER TABLE tecnicos ADD COLUMN valor_deslocamento DECIMAL(10,2) DEFAULT 30.00;
-- ALTER TABLE tecnicos ADD COLUMN valor_por_km DECIMAL(10,2) DEFAULT 0.75;
-- ALTER TABLE tecnicos ADD COLUMN valor_hora_extra DECIMAL(10,2) DEFAULT 75.00;
